{
  "name": "jurnii-booking",
  "version": "1.0.0",
  "private": true,
  "description": "Progressive booking form + Zoho CRM / Google Calendar backend. Self-contained, copyable module.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "googleapis": "^140.0.1",
    "jsonwebtoken": "^9.0.2"
  }
}
