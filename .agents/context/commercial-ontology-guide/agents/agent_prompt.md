Map the commercial ontology of https://TARGET-URL.com.

Crawl the site and inventory all existing commercial content. Apply the Commercial Ontology Framework as the organising layer and produce six structured outputs:

1. Entity Map — classify all content into Brand, Products, Features, Solutions, Use Cases, Personas. Record source names (as-is) and framework names (normalised).
2. Architecture Map — classify every page into the four-pillar content model (Products, Features, Solutions, Use Cases). Record entity placement and cross-links.
3. Copy-Grammar Map — classify all H1s, nav labels, and CTAs by grammar type. Identify compositions and map tone to persona types.
4. CRM Mapping — map entities to CRM fields, page interactions to CRM events. Generate the campaign coverage matrix and qualification gate definitions.
5. Compound Effects Projection — trace what each domain (site, pipeline, marketing, team, asset) looks like given the current structure. Cite measurements from the prior maps.
6. Transition Costs — model the effort to fully adopt the framework. Derive all estimates from concrete counts in the prior maps.

Produce the final synthesis at /mnt/user/outputs/00-ontology-synthesis.md.
